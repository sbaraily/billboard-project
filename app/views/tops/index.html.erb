<h1>Billboard Top Charts</h1>
<%= link_to "Add New Song", new_song_path %>
<%= link_to "Add New Artist", new_artist_path %>
<%= link_to "Add New Chart", new_top_path	 %>

<% @tops.each do |top| %>
  <%= link_to top_path(top.id)%>
<% end %>